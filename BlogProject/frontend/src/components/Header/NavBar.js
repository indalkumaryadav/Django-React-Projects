import React from "react";
import { AppBar, Button, Toolbar } from "@material-ui/core";

const NavBar = () => {
  return (
    <>
      <AppBar>
        <Button>indal</Button>
      </AppBar>
    </>
  );
};

export default NavBar;
