import React from "react";
import { AppBar, Button, Toolbar } from "@material-ui/core";
// import {} from "./style";

const NavBar = () => {
  return (
    <AppBar>
      <Toolbar>
        <Button>Indal</Button>
      </Toolbar>
    </AppBar>
  );
};

export default NavBar;
