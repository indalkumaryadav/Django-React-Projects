import SideBarOption from "./SideBarOption";

const SideBar = () => {
  return (
    <div>
      <h1>SideBar</h1>
      <SideBarOption />
      <SideBarOption />
      <SideBarOption />
      <SideBarOption />
    </div>
  );
};

export default SideBar;
